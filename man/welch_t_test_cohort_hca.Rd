% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference-cohort.R
\name{welch_t_test_cohort_hca}
\alias{welch_t_test_cohort_hca}
\title{Welch t-test of cohort log(mu) against HCA posterior draws}
\usage{
welch_t_test_cohort_hca(
  cohort_est,
  hca_draws,
  cohorts = NULL,
  exclude_groups = "reference",
  alpha = 0.05
)
}
\arguments{
\item{cohort_est}{A data frame from [estimate_cohort_logmu()] or
[bootstrap_cohort_logmu_batch()] with columns `group`, `log_mu`, and `se`.}

\item{hca_draws}{A numeric vector of posterior draws, or the list returned
by [expr_draws()] / [expr_predict()].}

\item{cohorts}{Optional character vector of cohort labels to test. Defaults
to all groups in `cohort_est` except those in `exclude_groups`.}

\item{exclude_groups}{Groups omitted when `cohorts` is `NULL` (default
`"reference"`).}

\item{alpha}{Significance level used for the `direction` column.}
}
\value{
A data frame with one row per tested cohort (see
  [compare_logmu_to_baseline()]).
}
\description{
Compares cohort estimates from [estimate_cohort_logmu()] to healthy HCA
posterior draws using [compare_logmu_to_baseline()]. By default, tests
every cohort in `cohort_est` except `exclude_groups`.
}
\details{
For a manual workflow, extract summaries with [cohort_estimate_at()] and
[summarize_posterior_draws()], then call [welch_test_means()] or
[compare_logmu_to_baseline()] directly.
}
