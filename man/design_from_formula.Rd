% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort.R
\name{design_from_formula}
\alias{design_from_formula}
\title{Build a design matrix from a one-sided formula}
\usage{
design_from_formula(formula, metadata)
}
\arguments{
\item{formula}{One-sided model formula, e.g. `~ 0 + Category` or `~ 1`.}

\item{metadata}{A `data.frame` of sample covariates (one row per library).
Must contain every variable named in `formula`.}
}
\value{
A numeric design matrix with one row per sample and cleaned
  column names (group labels).
}
\description{
Core helper used by [estimate_cohort_logmu()]. Converts a formula such as
`~ 0 + Category` into a numeric design matrix whose column names are the
cohort / term labels used in the tidy output of [estimate_logmu_ql()].
}
\seealso{
[estimate_logmu_ql()], [estimate_cohort_logmu()]
}
