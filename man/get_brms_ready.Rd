% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utlis.R
\name{get_brms_ready}
\alias{get_brms_ready}
\title{Download a gene-level brms fit for a cell type}
\usage{
get_brms_ready(
  cell_type,
  gene_ensg,
  version = "latest",
  cache_directory = get_default_cache_dir(),
  use_cache = TRUE
)
}
\arguments{
\item{cell_type}{Character. Cell type name.}

\item{gene_ensg}{Character. Ensembl gene ID (object name, no extension).}

\item{version}{`"latest"` or a container/version pin (e.g. `"V1"`).}

\item{cache_directory}{Character. Local cache directory.}

\item{use_cache}{Logical. Reuse a previously downloaded file.}
}
\value{
Same list as [get_file_ready()], plus `cell_type`, `container`, `ver_no`.
}
\description{
Looks up the version container from `meta/latest.csv` (or a pinned version)
and downloads `V{n}/{cell_type}/{gene_ensg}`.
}
