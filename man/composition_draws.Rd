% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composition-query.R
\name{composition_draws}
\alias{composition_draws}
\title{Draw posterior predictive cell-type proportions from a sccomp fit}
\usage{
composition_draws(
  fit,
  newdata = NULL,
  sample_id = "query_sample",
  age_decade = NA,
  sex = NA,
  ethnicity_groups = NA,
  assay_groups = NA,
  tissue_groups = NA,
  summary_instead_of_draws = FALSE,
  ...
)
}
\arguments{
\item{fit}{A `posteriorHCA_sccomp_fit` object from [load_sccomp_fit()], or a
fitted sccomp object.}

\item{newdata}{Optional sample metadata with columns `sample_id`,
`age_decade`, `sex`, `ethnicity_groups_imputed`, `assay_groups___altered`,
`dataset_id___altered`, and `tissue_groups`. When omitted, a one-row table
is built from the scalar metadata arguments below.}

\item{sample_id}{Sample identifier used when `newdata` is `NULL`.}

\item{age_decade, sex, ethnicity_groups, assay_groups, tissue_groups}{Metadata
choices for the query profile. Use `NA` to marginalise over that covariate.}

\item{summary_instead_of_draws}{Passed to [sccomp::sccomp_predict()].}

\item{...}{Additional arguments passed to [sccomp::sccomp_predict()].}
}
\value{
A list with `draws` (tibble of posterior predictive proportions),
  `newdata`, `release`, and `model`.
}
\description{
Uses the sccomp model's full composition formula. Unknown covariates can be
left as `NA`; sccomp marginalises over them automatically.
}
