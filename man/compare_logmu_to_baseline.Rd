% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference-welch.R
\name{compare_logmu_to_baseline}
\alias{compare_logmu_to_baseline}
\title{Compare one cohort log(μ) estimate to a baseline}
\usage{
compare_logmu_to_baseline(
  mu,
  se,
  n = NULL,
  baseline_draws = NULL,
  baseline_mu = NULL,
  baseline_se = NULL,
  baseline_n = NULL,
  cohort = "cohort",
  method = "ql",
  gene = NA_character_,
  gene_symbol = NA_character_,
  cell_type = NA_character_,
  alpha = 0.05,
  alternative = c("two.sided", "greater", "less")
)
}
\arguments{
\item{mu, se}{Cohort point estimate and standard error (typically `log_mu`
and `se` from [estimate_cohort_logmu()]).}

\item{n}{Optional cohort sample size for Welch degrees of freedom.}

\item{baseline_draws}{Numeric vector or [expr_draws()] output for the
reference baseline.}

\item{baseline_mu, baseline_se, baseline_n}{Baseline summary when draws are
not supplied.}

\item{cohort}{Optional cohort label stored in the output.}

\item{method}{Optional method label (`"ql"`, `"bootstrap"`, etc.).}

\item{gene, gene_symbol, cell_type}{Optional annotation columns.}

\item{alpha}{Significance level for the `direction` column.}

\item{alternative}{`"two.sided"` (default), `"greater"` (mu1 > mu2), or
`"less"` (mu1 < mu2).}
}
\value{
A one-row data frame compatible with [welch_t_test_cohort_hca()]
  and [plot_cohort_vs_hca()].
}
\description{
Low-level comparison helper combining [welch_test_means()],
[summarize_posterior_draws()], and empirical rank. Supply either
`baseline_draws` or explicit `baseline_mu`, `baseline_se`, and optionally
`baseline_n`.
}
