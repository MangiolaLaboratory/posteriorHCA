% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_cohort_vs_hca}
\alias{plot_cohort_vs_hca}
\title{Plot cohort log(mu) estimates against healthy HCA posterior draws}
\usage{
plot_cohort_vs_hca(
  hca_draws,
  cohort_est,
  exclude_groups = character(0),
  show_se = TRUE,
  stagger_heights = TRUE,
  colour_by = c("cohort", "direction"),
  cohort_palette = NULL,
  annotate = c("group", "p_value"),
  fill = "#4C78A8",
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{hca_draws}{Posterior draws from [expression_draws()].
Must use `quantity = "linpred"` because cohort tests are on log(mu).}

\item{cohort_est}{Data frame with cohort estimates (`group`, `log_mu`, ...).}

\item{exclude_groups}{Character vector of cohort labels to omit.}

\item{show_se}{If `TRUE`, draw horizontal error bars for cohort `log_mu +/- se`.}

\item{stagger_heights}{If `TRUE`, place each cohort marker at a different
height above the density to reduce overlap.}

\item{colour_by}{Colour cohort markers by `"cohort"` or `"direction"`.}

\item{cohort_palette}{Optional colour vector when `colour_by = "cohort"`.}

\item{annotate}{Character vector of label fields: `"group"`, `"p_value"`,
`"direction"`, `"empirical_rank"`, `"method"`.}

\item{fill}{Fill colour for the HCA density curve.}

\item{title}{Plot title. Default is built from gene / cell-type metadata.}

\item{subtitle}{Optional subtitle.}
}
\value{
A `ggplot` object.
}
\description{
Visualises cohort log(mu) estimates against healthy HCA posterior draws.
Pass a data frame with at least `group` and `log_mu` (and optionally `se`,
`p_value`, `direction`).
}
