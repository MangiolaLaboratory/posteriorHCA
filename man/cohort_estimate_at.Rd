% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference-welch.R
\name{cohort_estimate_at}
\alias{cohort_estimate_at}
\title{Extract one cohort estimate row for manual testing}
\usage{
cohort_estimate_at(cohort_est, group = NULL, row = NULL)
}
\arguments{
\item{cohort_est}{Output from [estimate_cohort_logmu()] or
[bootstrap_cohort_logmu_batch()].}

\item{group}{Cohort label to extract. Required when `cohort_est` has
more than one row.}

\item{row}{Optional row index when `group` is not supplied.}
}
\value{
A list with `group`, `mu` (`log_mu`), `se`, `n`, `method`, and
  metadata fields from [cohort_est_metadata()].
}
\description{
Extract one cohort estimate row for manual testing
}
