% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort.R
\name{calculate_tmm_scaling}
\alias{calculate_tmm_scaling}
\title{TMM / TMMwsp effective library sizes with an HCA reference}
\usage{
calculate_tmm_scaling(counts, reference_name, method = "TMMwsp")
}
\arguments{
\item{counts}{Gene-by-sample count matrix that already includes the
reference column.}

\item{reference_name}{Name of the reference column.}

\item{method}{Passed to [edgeR::calcNormFactors()]. Default `"TMMwsp"`.}
}
\value{
A list with `norm_factors`, `library_size`, `effective_size`,
  `log_effective_library_size`, `hca_effective_library_size`,
  `hca_log_effective_library_size`, `multiplier`, `reference_name`, and
  `method`. `multiplier` is `E_H / E_j` (descriptive only).
}
\description{
Runs [edgeR::calcNormFactors()] jointly on user libraries plus the HCA
reference (`refColumn = reference_name`). Returns effective library sizes on
that shared scale. The edgeR fitting offset for user samples is
`log(effective_size)`. HCA-scale reporting uses
`hca_log_effective_library_size` after model fitting; it is not folded into
the edgeR offset.
}
\seealso{
[merge_with_reference_sample()], [scale_to_hca_reference()]
}
