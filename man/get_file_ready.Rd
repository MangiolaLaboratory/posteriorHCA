% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utlis.R
\name{get_file_ready}
\alias{get_file_ready}
\title{Get file from Nectar object storage}
\usage{
get_file_ready(
  cache_directory = get_default_cache_dir(),
  use_cache = TRUE,
  container,
  prefix,
  filename
)
}
\arguments{
\item{cache_directory}{Character. Directory to store the cached file. Defaults to the standard cache directory.}

\item{use_cache}{Logical. If TRUE, uses the cached version if available.}

\item{container}{Character. Name of the container in Nectar object storage.}

\item{prefix}{Character. Prefix (folder name) in the container.}

\item{filename}{Character. Name of the file to download.}
}
\value{
List. Always returns a list with 'status' (success/not_found/error/empty), 'path' (local file path if successful, NULL otherwise), 'url' (requested URL), and optionally 'error' (error message for error cases).
}
\description{
Downloads and caches a file from Nectar object storage using direct URL downloads.
The file is synchronized to the cache directory maintaining the same structure as in the object store.
}
