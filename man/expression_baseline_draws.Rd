% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expr-query.R
\name{expression_baseline_draws}
\alias{expression_baseline_draws}
\title{Posterior draws for an HCA expression baseline query}
\usage{
expression_baseline_draws(
  cell_type = NULL,
  gene_ensg = NULL,
  fit = NULL,
  age_decade = NA,
  sex = NA,
  disease_groups = NA,
  ethnicity_groups = NA,
  assay_groups = NA,
  tissue_groups = NA,
  dataset_id = NA,
  offset = 0,
  new_study_id = "__new_study__",
  quantity = c("linpred", "predict", "epred"),
  marginalise = c("mean", "pool", "sample"),
  ndraws = NULL,
  transform = FALSE,
  re_formula = NULL,
  allow_new_levels = TRUE,
  sample_new_levels = "gaussian",
  seed = NULL,
  version = "latest",
  cache_directory = get_default_cache_dir(),
  use_cache = TRUE
)
}
\arguments{
\item{cell_type}{Cell type name (required when `fit` is `NULL`).}

\item{gene_ensg}{Ensembl gene id (required when `fit` is `NULL`).}

\item{fit}{Optional pre-loaded fit from [load_expression_fit()].}

\item{age_decade, sex, disease_groups, ethnicity_groups, assay_groups, tissue_groups}{Passed to [build_newdata_grid()].}

\item{dataset_id, offset, new_study_id}{Passed to [build_newdata_grid()].}

\item{quantity, marginalise, ndraws, transform, re_formula, allow_new_levels, sample_new_levels, seed}{Passed to [expression_draws()].}

\item{version}{`"latest"` or a container/version pin (e.g. `"V1"`).}

\item{cache_directory}{Character. Local cache directory.}

\item{use_cache}{Logical. Reuse a previously downloaded file.}
}
\value{
A list from [expression_draws()].
}
\description{
Convenience wrapper around [load_expression_fit()],
[build_newdata_grid()], and [expression_draws()]. Loads the atlas model
when `fit` is not supplied, builds the covariate grid, and returns the
usual [expression_draws()] list. Does not call brms posterior helpers
directly and does not summarise or test draws.
}
\seealso{
[load_expression_fit()], [build_newdata_grid()],
  [expression_draws()], [compare_cohort_to_hca()]
}
