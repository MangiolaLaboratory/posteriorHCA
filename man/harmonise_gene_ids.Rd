% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene-id.R
\name{harmonise_gene_ids}
\alias{harmonise_gene_ids}
\title{Harmonise gene identifiers to canonical ENSG ids}
\usage{
harmonise_gene_ids(
  x,
  cell_type = NULL,
  version = "latest",
  universe = NULL,
  orgdb = NULL,
  id_type = c("auto", "ensembl", "symbol"),
  strict = FALSE,
  assay = NULL,
  cache_directory = get_default_cache_dir(),
  use_cache = TRUE
)
}
\arguments{
\item{x}{A matrix, `Matrix`, `SummarizedExperiment`, `Seurat` object, or
aligned list from [scale_to_hca_reference()].}

\item{cell_type}{Optional cell type for universe validation.}

\item{version}{Nectar version pin passed to [get_gene_universe()].}

\item{universe}{Optional preloaded ENSG universe. Overrides `cell_type`.}

\item{orgdb}{Optional `OrgDb` object for symbol mapping. Defaults to
`org.Hs.eg.db` when installed.}

\item{id_type}{`"auto"` guesses from the input pattern; `"ensembl"` treats
inputs as Ensembl ids; `"symbol"` maps symbols.}

\item{strict}{If `TRUE`, error when a gene cannot be resolved or is absent
from the universe.}

\item{assay}{Assay name for SE / Seurat input. Default `"counts"` when present.}

\item{cache_directory, use_cache}{Passed to [get_gene_universe()] when needed.}
}
\value{
An object of the same class as `x` with harmonised ENSG feature names.
}
\description{
Maps gene symbols to Ensembl ids and drops unmapped or duplicated genes.
The output container matches the input class: `Seurat` in gives `Seurat`
out, `SummarizedExperiment` in gives `SummarizedExperiment` out, and a
matrix or aligned list is returned in the same form.
}
