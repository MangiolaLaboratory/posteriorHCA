% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference-welch.R
\name{welch_test_means}
\alias{welch_test_means}
\title{Heteroscedastic Welch test for the difference between two mean estimates}
\usage{
welch_test_means(
  mu1,
  se1,
  mu2,
  se2,
  n1 = NULL,
  n2 = NULL,
  alternative = c("two.sided", "greater", "less")
)
}
\arguments{
\item{mu1, mu2}{Point estimates of the two means.}

\item{se1, se2}{Standard errors (or posterior SDs) associated with `mu1`
and `mu2`.}

\item{n1, n2}{Optional sample or draw counts used for Welch–Satterthwaite
degrees of freedom.}

\item{alternative}{`"two.sided"` (default), `"greater"` (mu1 > mu2), or
`"less"` (mu1 < mu2).}
}
\value{
A list with `mu1`, `se1`, `n1`, `mu2`, `se2`, `n2`, `delta`
  (`mu1 - mu2`), `se_diff`, `t_stat`, `df`, and `p_value`.
}
\description{
Compares two estimated means on the same scale (typically latent log(μ))
using a Welch-style statistic. When `n1` and `n2` are supplied and both
are at least 2, p-values use the Welch–Satterthwaite t distribution;
otherwise a normal approximation is used.
}
